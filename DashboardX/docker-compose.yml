version: '1'
services:
  webassembly-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"  
  presentation:
    image: presentation
    build:
      context: .
      dockerfile: Presentation/Dockerfile